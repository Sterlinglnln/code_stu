// logical operators
#include <iostream>
using namespace std;

int main() {
    int a = 10, b = 20;
    cout << "!a = " << !a << endl; // Logical NOT operator
    cout << "a && b = " << (a && b) << endl; // Logical AND operator
    cout << "a || b = " << (a || b) << endl; // Logical OR operator
}